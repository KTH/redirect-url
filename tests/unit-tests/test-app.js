/* eslint-env mocha */
"use strict";

// Testing libraries
const expect = require("chai").expect;
const { assert } = require("chai");

describe("Redirect.\n", function () {
  it("You can specify an absolute url (i.e: https://www.kth.se/my-path/) to redirect all traffic to", function () {
    expect(true).to.equal(true);
  });
  it("You can specify a relative url  (i.e: /my-path/) to redirect all traffic to", function () {
    expect(true).to.equal(true);
  });
});
